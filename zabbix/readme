Из окна терминала, откуда запускаются скрипты для работы zabbix_api, вручную экспортировать
export Z_TOKEN=<токен>
эта переменная будет уничтожена вместе с окном терминала, откуда она была установлена
