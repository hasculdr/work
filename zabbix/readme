Несколько скриптов для zabbix

Для работы с zabbix api из окна терминала, откуда запускаются скрипты, вручную экспортировать
export Z_TOKEN=<токен>
Переменная будет уничтожена вместе с окном терминала, откуда она была объявлена.

smartctl* - сбор данных smart и отправка zabbix sender'ом
